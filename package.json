{
  "name": "find-my-nearest",
  "version": "1.0.0",
  "description": "A JavaScript project that uses LeafletJS to find nearby places like hospitals, restaurants, and gas stations. It makes use of geolocation and gets the user's current location, if allowed.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tailwindcss -i ./css/style.css -o ./src/style-output.css --watch"
  },
  "author": "Gabriel Austria",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.4.17"
  }
}
